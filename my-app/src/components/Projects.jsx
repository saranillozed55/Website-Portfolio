import {motion} from "motion/react"

function Projects() {

    return(
        <section
        id = "projects"
        className = "w-full max-w-4xl mx-auto px-6 mt-40"
        >
            <h2 className =
            "text-4xl">
                / Projects
            </h2>

        </section>

    );
}
export default Projects